To build application for sending emails to users with dulpicated accounts type in cmd:
'mvn clean install -Psend-mails'

For run the application type in cmd:
'java -jar  jarfile.jar file.properties'
where file.properties is a file with properties for sending (see example in resources)
